@model IEnumerable<DroneWebApp.Models.DroneFlight>

@{
    ViewBag.Title = "Drone Flight Index";
}

<br />
<h2 class="textAlignCenter">Drone Flights</h2>

<h4>@Html.ActionLink("Create new Flight", "Create", "", new { @class = "btn-info btn-lg whiteText" })</h4>
<hr />

<table id="dftable" class="ui celled table table-hover table-striped textAlignCenter" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Project")
            </th>
            <th>
                @Html.DisplayName("Location")
            </th>
            <th>
                @Html.DisplayName("Date")
            </th>
            <th>
                @Html.DisplayName("Drone")
            </th>
            <th>
                @Html.DisplayName("Pilot")
            </th>
            <th>Files</th>
            <th></th>
            <th>ArcGIS</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Project.ProjectCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @item.Date.ToString("dd/MM/yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Drone.DroneName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pilot.PilotName)
                </td>
                <td>
                    @if (!item.hasQR)
                    {
                        @Html.ActionLink("QR", "QualityReport", new { id = item.FlightId }, new { @class = "btn btn-danger btn-sm whiteText" })
                    }
                    else
                    {
                        @Html.ActionLink("QR", "QualityReport", new { id = item.FlightId }, new { @class = "btn btn-success btn-sm whiteText" })
                    }
                    @if (!item.hasGCPs)
                    {
                        @Html.ActionLink("GCP", "GCPPoints", new { id = item.FlightId }, new { @class = "btn btn-danger btn-sm whiteText" })
                    }
                    else
                    {
                        @Html.ActionLink("GCP", "GCPPoints", new { id = item.FlightId }, new { @class = "btn btn-success btn-sm whiteText" })
                    }
                    @if (!item.hasCTRLs)
                    {
                        @Html.ActionLink("CTRL", "CTRLPoints", new { id = item.FlightId }, new { @class = "btn btn-danger btn-sm whiteText" })
                    }
                    else
                    {
                        @Html.ActionLink("CTRL", "CTRLPoints", new { id = item.FlightId }, new { @class = "btn btn-success btn-sm whiteText" })
                    }
                    @if (!item.hasTFW)
                    {
                        @Html.ActionLink("TFW", "TFW", new { id = item.FlightId }, new { @class = "btn btn-danger btn-sm whiteText" })
                    }
                    else
                    {
                        @Html.ActionLink("TFW", "TFW", new { id = item.FlightId }, new { @class = "btn btn-success btn-sm whiteText" })
                    }
                    <br />
                    <p></p>
                    @if (!item.hasXYZ)
                    {
                        <div class="btn btn-danger btn-sm disabled">
                            <span>
                                XYZ
                            </span>
                        </div>
                    }
                    else
                    {
                        <div class="btn btn-success btn-sm disabled">
                            <span>
                                XYZ
                            </span>
                        </div>
                    }
                    @if (!item.hasDroneLog)
                    {
                        <div class="btn btn-danger btn-sm disabled">
                            <span>
                                Drone Log
                            </span>
                        </div>
                    }
                    else
                    {
                        <div class="btn btn-success btn-sm disabled">
                            <span>
                                Drone Log
                            </span>
                        </div>
                    }
                </td>
                <td>
                    @Html.ActionLink("Upload File", "Index", "Files", new { id = item.FlightId }, new { @class = "btn btn-info btn-sm whiteText" })
                </td>
                <td>
                    @Html.ActionLink("View Flight", "ViewMap", "Map", new { id = item.FlightId }, new { @class = "btn btn-primary btn-sm whiteText" })
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.FlightId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.FlightId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.FlightId })
                </td>
            </tr>
        }
    </tbody>
</table>
